<style>
    .admin-nav {
        display: flex;
        gap: 10px;
        margin-bottom: 20px;
        border-bottom: 1px solid var(--border-color);
        padding-bottom: 15px;
    }
    .admin-nav a {
        padding: 8px 15px;
        border-radius: 5px;
        text-decoration: none;
        background-color: <%= page === 'active_page' ? 'var(--primary-accent)' : 'var(--dark-bg-tertiary)' %>;
        color: var(--text-primary);
    }
</style>
<div class="admin-nav">
    <a href="/admin" style="<%= page === 'matches' ? 'background-color: var(--primary-accent);' : '' %>"><i class="fas fa-futbol"></i> Matches</a>
    <a href="/admin/highlights" style="<%= page === 'highlights' ? 'background-color: var(--primary-accent);' : '' %>"><i class="fas fa-star"></i> Highlights</a>
    <a href="/admin/channels" style="<%= page === 'channels' ? 'background-color: var(--primary-accent);' : '' %>"><i class="fas fa-tv"></i> 24/7 Channels</a>
</div>