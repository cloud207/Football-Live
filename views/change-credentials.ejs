<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Credentials - Admin Panel</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="admin-container">
        <div class="admin-header">
            <h1>Change Credentials</h1>
            <a href="/admin" class="btn btn-cancel">Back to Dashboard</a>
        </div>

        <% if (typeof message !== 'undefined' && message) { %>
            <p style="color: #28a745; background-color: #d4edda; padding: 10px; border-radius: 5px; margin-bottom: 15px;"><%= message %></p>
        <% } %>
        <% if (typeof error !== 'undefined' && error) { %>
            <p style="color: #dc3545; background-color: #f8d7da; padding: 10px; border-radius: 5px; margin-bottom: 15px;"><%= error %></p>
        <% } %>

        <form action="/admin/change-credentials" method="POST" class="match-form">
            <div class="form-group" style="grid-template-columns: 1fr;">
                <label for="current_password">Current Password</label>
                <input type="password" id="current_password" name="current_password" placeholder="Enter your current password" required>
            </div>
            <div class="form-group" style="grid-template-columns: 1fr;">
                <label for="new_username">New Username</label>
                <input type="text" id="new_username" name="new_username" placeholder="Enter new username (leave blank to keep current)" >
            </div>
            <div class="form-group" style="grid-template-columns: 1fr;">
                <label for="new_password">New Password</label>
                <input type="password" id="new_password" name="new_password" placeholder="Enter new password (leave blank to keep current)">
            </div>
            <button type="submit">Update Credentials</button>
        </form>
    </div>
</body>
</html>